# Agent Working Procedure

이 저장소에서 요구사항을 처리할 때 에이전트는 아래 절차를 기본 작업 흐름으로 따른다.

## Required Workflow

1. 요구사항을 분석하고, 구현 가능한 작업 단계로 분해한다.
2. 각 작업 단계를 순서대로 착수한다.
3. 각 단계가 완료될 때마다 결과를 검토한다.
4. 검토 중 이상 징후, 오류, 누락, UI/UX 불일치, 테스트 실패, 배포 반영 누락이 발견되면 즉시 수정한다.
5. 수정 후 같은 검토를 다시 수행하며, 이상 징후나 오류가 없어질 때까지 반복한다.
6. 모든 작업 단계가 완료되면 전체 결과를 다시 검토한다.
7. 전체 검토에서 이상 징후나 오류가 발견되면 수정과 재검토 루프를 반복한다.
8. 모든 검토가 통과하면 메인 브랜치에 커밋하고 원격 저장소로 푸쉬한다.
9. 변경사항이 런타임에 반영되어야 하는 경우 Docker 컨테이너를 재기동하거나 재빌드 후 재기동한다.
10. 컨테이너 반영 후 health check, 실제 페이지/기능 확인, 필요한 경우 브라우저 검증까지 수행한다.

## Completion Criteria

작업은 아래 조건을 모두 만족해야 완료로 본다.

- 요구사항에 포함된 모든 단계가 처리되어야 한다.
- 단계별 검토와 전체 검토가 완료되어야 한다.
- 확인된 이상 징후와 오류는 남겨두지 않아야 한다.
- 필요한 테스트와 검증이 통과해야 한다.
- 커밋과 푸쉬가 완료되어야 한다.
- 런타임 변경은 Docker 컨테이너에 반영되어야 한다.

문서만 변경되는 작업처럼 런타임 컨테이너에 반영될 내용이 없는 경우에도, 그 사실을 명시하고 필요한 저장소 검증과 커밋/푸쉬를 완료한다.
